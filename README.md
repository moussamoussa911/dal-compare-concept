# Dal Compare – Website Concept

Modern, multilingual concept website for Dal Compare in Ochtrup.

## Local

```bash
npm install
npm start
```

The server listens on `PORT` or `4273` and exposes `/health`.

## Deployment

Configured as a Render web service in the Frankfurt region via `render.yaml`.
